FROM python:3.11-slim

# Install system dependencies
RUN apt-get update && apt-get install -y \
    git \
    curl \
    docker.io \
    && rm -rf /var/lib/apt/lists/*

# Install mini-swe-agent with dependencies
RUN pip install --no-cache-dir mini-swe-agent datasets huggingface_hub

WORKDIR /app

# Copy configuration
COPY config/mini-swe-agent/ /app/config/

# Create results directory mount point
RUN mkdir -p /results

# Default entrypoint for SWE-bench runs
ENTRYPOINT ["mini-extra", "swebench"]
