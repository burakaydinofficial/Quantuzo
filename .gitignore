# Models (large GGUF files at root level only)
/models/
*.gguf

# Results - large intermediate files only (we commit final results)
#results/*/testbed/
#results/*/logs/
#results/*/*.log

# Exclude all results from the repo
results/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
venv/
.venv/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker
.docker/

# Logs
*.log

# Environment overrides
.env
.env.local

# Local runtime config overrides (users may customize)
spec/runtime.local.conf

# Generated at runtime by run.sh
config/mini-swe-agent/registry.json

# Locally-generated leaderboard (rebuilt from summaries; the real one lives on HF)
/leaderboard*.jsonl

# Scratch area for reviewing HuggingFace PRs (clones + downloaded run artifacts)
/hf-pr-review/

# Claude
CLAUDE.md
.claude/

# Stray local clone of the agent source (installed via pip in the images, not from here)
/mini-swe-agent/
